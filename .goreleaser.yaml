version: 2
project_name: deepseek-orca

before:
  hooks:
    - go mod download

builds:
  - id: deepseek-orca
    main: ./cmd/deepseek-orca
    binary: deepseek-orca
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{ .Tag }}
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]

archives:
  - id: deepseek-orca
    ids: [deepseek-orca]
    name_template: "deepseek-orca-{{ .Os }}-{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: SHA256SUMS
  algorithm: sha256

homebrew_casks:
  - name: deepseek-orca
    ids: [deepseek-orca]
    # Prereleases (v*-rc*) must not update the stable tap; brew has no separate
    # prerelease channel, so a pushed rc cask becomes the default `brew install`.
    # npm is a separate line (release-npm.yml, `npm-v*`), so an npm prerelease no
    # longer drags brew along — a stable `v*` cask can ship while npm stays on rc.
    skip_upload: "auto"
    repository:
      owner: esengine
      name: homebrew-deepseek-orca
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/nanbo0ne/DeepSeek-Orca"
    description: "Cache-first DeepSeek coding agent for the terminal."
    commit_author:
      name: deepseek-orca
      email: deepseek-orca@deepseek.com
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/deepseek-orca"]
          end

release:
  prerelease: auto
  name_template: "DeepSeek-Orca CLI {{ .Tag }}"
